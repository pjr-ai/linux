今天练了日志排查   

LOG=/var/log/secure
grep -ci "failed password" "$LOG"
下面两个部分卡住一个是查时间一个是查ip
grep -i "failed password" "$LOG" | grep -F "$(LC_ALL=C date '+%b %e')"
grep -i "failed password" "$LOG" | grep -oE "([0-9]{1,3}\.){3}[0-9]{1,3}" | sort | uniq -c | sort -nr