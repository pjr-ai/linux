# Linux 10 题清障卷 · 答卷

> 交卷口令：**「清障卷交了」** → 导师核文件 + 随机抽 3 题当场重敲 + 口述
> 判分：**≥7 题「独立」= 及格**；讲不清的从「独立」降级；抄答案 / 虚标 = 该题记 0
> 纪律：①开考前先打快照 ②每题在 CentOS7 里**真敲**，命令照实抄（不美化、不补全）③卡住就写「卡住」——假「独立」比卡住亏得多

---

## 交卷前自检

- [ ] 10 题四件齐全：命令 / 一句解释 / 自评 / 错在哪·怎么修
- [ ] 每个标「独立」的题，能合上资料重敲一遍
- [ ] 卡住的题写清卡在哪一步（导师据此补讲，不扣分）
- [ ] 加分题（选做）写了吗

---

## 1. 建多级目录并树形列出
- 命令：mkdir -pv 1/2/3
mkdir: 已创建目录 "1"
mkdir: 已创建目录 "1/2"
mkdir: 已创建目录 "1/2/3"
tree 1/2/3/4
- 一句解释：mkdir -p 创建多级目录
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：没错

## 2. 文件设 644、目录设 755，解释数字含义
- 命令：
[root@localhost ~]# chmod -R 755 /1/2
- 一句解释：644是代表u有写
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 3. 找出 /var/log 下 7 天内修改且 >1MB 的文件
- 命令：find /var/log -type f -mtime 7 -size +1M
- 一句解释：-mtime 7是七天以内  -size +1M 是大于1M的文件
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 4. 用两种方法查 22 端口被谁占用
- 方法一命令：ss -tnlp 
- 方法二命令：losf -i
- 一句解释：ss -tnlp 内核 socket 表   losf -i 从进程打开的文件查
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 5. 实时盯日志并过滤 error 行
- 命令：tail -f /var/log/auth.log  | grep "error"
- 一句解释：-f 是实时爬在日志上 -v是除去
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 6. 统计日志 error 行数并保存到文件
- 命令：grep -c /var/log/auth.log >/1/1.txt
- 一句解释：-c是统计  >是把内容打印到文件
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 7. df / free / uptime 各看一项并解释一个数字
- 命令：df 
- 我解释的数字：
- 自评：☐ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 8. systemctl 启停服务、查状态、设开机自启
- 命令：systemctl start 
systemctl stop
 systemctl status
 systemctl enable
- 一句解释：命令后面加进程名
- 自评：√独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 9. 找出 CPU 占用最高的进程并杀掉（sleep 模拟）
- 造进程的命令：sleep 1000 &
- 定位命令：ps -ef | grep sleep
- 杀进程命令：kill -9 sleep
- 一句解释：-9是强制杀死
- 自评：☐ 独立　√ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## 10. 打包 /etc 到 /backup 并验证解压
- 命令：tar -zcvf  etc.tar.gz /etc
tar -zxvf /etc etc.tar.gz -C /backup

- 一句解释：先打包再解压
- 自评：√ 独立　☐ 查资料　☐ 卡住
- 错在哪 / 怎么修：

## ＋ 加分题：③＋⑩ 合写 ≤15 行 shell 脚本（选做）
- 脚本：vim 1.sh
#!/bin/bash
find /var/log -type f -mtime 7 -size +1M
tar -zcvf  etc.tar.gz /var/log
tar -zxvf /var/log etc.tar.gz -C /backup

- 一句解释：创脚本要先写头
- 自评：√ 独立　☐ 查资料　☐ 卡住

---

## 提示与问答记录（卡住时找导师，给阶梯提示，不给答案）

| 题号 | 我卡在哪 | 导师给的提示 | 我最终怎么解 |
|---|---|---|---|
| 9 | 不知道用什么命令找 | top 按 CPU 排序 / ps 排序参数 / kill 的两个信号 | |


