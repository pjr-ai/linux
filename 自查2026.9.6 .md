# Linux 面试复习 · 错题本

> 来源：20题综合自测 | 配套教材：韩顺平 Linux
> 用途：记录答错/混淆的知识点，重点回看

---

## 🔥 高频重灾区（最该先看）

### 1. tar 打包解压（曾连续3次答错，重中之重）
```bash
# 打包（c=create创建）
tar -zcvf 包名.tar.gz 要打包的内容
tar -zcvf demo.tar.gz a.txt folder1

# 解压到当前目录（x=extract抽取）
tar -zxvf demo.tar.gz

# 解压到指定目录（注意：-C 大写！放包名后面！）
tar -zxvf demo.tar.gz -C /home
```
- ❌ 错误写法：`gizp`（没有这命令）、`-C` 写成小写 `c`、`-C` 放错位置
- ✅ 口诀：**c打包、x解压、C指定目录（大写）、f后跟包名**

### 2. chmod vs chown（曾搞反！）
| 命令 | 管什么 | 例子 |
|---|---|---|
| `chmod` | 改**权限**（rwx读写执行）| `chmod 754 test.txt` |
| `chown` | 改**归属**（所有者:组）| `chown root:root test.txt` |

### 3. 递归参数的大小写（常混！）
| 命令 | 参数 | 说明 |
|---|---|---|
| `cp -r` | 小写 r | 复制文件夹 |
| `rm -rf` | 小写 r | 删文件夹 |
| `zip -r` | 小写 r | 压缩文件夹 |
| `chown -R` | **大写 R** | 递归改归属 |
| `chmod -R` | **大写 R** | 递归改权限 |

---

## 📕 逐题错题明细

### 错题1 · 家目录混淆
- 问：root 的家目录？
- 错答：`/`（根目录）
- 对：`/root`（root专属）
- 区分：`/`=根目录起点、`/root`=root的家、`/home/用户名`=普通用户的家

### 错题2 · 用户命令 vs 文件命令混用
- 问：创建用户tom 4步？
- 错答：mkdir/pwadd/cd/rm
- 对：`useradd tom` → `passwd tom` → `su - tom` → `userdel -r tom`
- 关键：管"人"用 useradd/userdel，管"文件"用 mkdir/rm/cd，**别串台**

### 错题3 · 文件查看命令
- less 能上下翻+搜索（不是grep！grep是搜内容不是翻页）
- head 看开头5行（head -5），不是 tail！head=头、tail=尾
- tail -f 退出按 Ctrl+C（不是 q！q是more/less的退出键）

### 错题4 · find 结构（老坑）
- 错答：`find -i /home *.txt`
- 对：`find /home -name "*.txt"`
- 要点：**路径 + -name + "通配符"**，-name 绝对不能漏

### 错题5 · grep -i 与默认大小写
- grep **默认区分大小写**（搜error找不到ERROR）
- 加 `-i` 才忽略大小写
- 混淆点：-i=忽略(ignore)、-v=反向(invert，找不含的)

### 错题6 · date / cal 不会
```bash
date                 # 当前时间
date "+%Y-%m-%d"     # 2026-09-01（%Y年 %m月 %d日 %H时 %M分 %S秒）
cal                  # 当月日历
cal 2026             # 全年日历（后面直接跟年份）
```

### 错题7 · tail -F 大写
- `-f` 小写：日志文件重建后跟丢
- `-F` 大写：日志文件重建后**继续跟**（日志轮转时用）

### 错题8 · Vim 快捷键混（曾把dd当撤销、esc当跳末尾）
| 操作 | 按键 |
|---|---|
| 粘贴 | `p`（不是rr）|
| 撤销 | `u`（不是dd，dd是删除行）|
| 跳文件末尾 | `G`（不是esc，esc是回正常模式）|
| 跳文件开头 | `gg` |
| 强制退出不保存 | `:q!`（带冒号！）|

### 错题9 · crontab 细节
- 追加日志用 `>>`（不是 `<<`，<<是输入重定向）
- 查看任务用 `crontab -l`（不是 -i！）
- 记忆：-e编辑、-l查看(look)、-r删除(remove)
- 任务行里**不能**写 crontab 命令本身

### 错题10 · mv / rm 理解
- `mv 旧名 新名`（同目录）= **重命名**，不是覆盖
- `rm -rf /` = 核弹命令，删整个系统无法恢复，严禁执行

### 错题11 · cp 不带 -r 的后果
- `cp dir1 /opt` 不带 -r → **报错**（略过目录），不是复制成空
- 必须 `cp -r dir1 /opt`

### 错题12 · 权限数字
- r=4、w=2、x=1（记住 421）
- rwx=7、rw-=6、r-x=5、r--=4

---

## ✅ 需要反复默写的三张表

### 表1：tar 四连
```bash
tar -zcvf 包名 内容    # 打包
tar -zxvf 包名         # 解压当前目录
tar -zxvf 包名 -C 目录  # 解压到指定目录（C大写）
```

### 表2：crontab
```bash
crontab -e  # 编辑
crontab -l  # 查看
crontab -r  # 删除
# 30 2 * * 1-5 命令 = 周一~周五2:30
# 分 时 日 月 周
```

### 表3：权限归属
```bash
chmod 754 test.txt          # 权限（u=7,g=5,o=4）
chown root:root test.txt    # 归属
chown -R root:root dir1     # 递归归属（R大写）
```