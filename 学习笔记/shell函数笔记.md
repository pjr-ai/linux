# 🐚 Shell 函数学习笔记（完整版）
### 今天的学习内容：从"为什么需要函数"到"函数参数/返回值/局部变量"全收录

---

## 📌 目录
- 第1章：函数是什么？为什么需要它？（今天最重要的"思想课"）
- 第2章：函数的定义语法
- 第3章：函数的调用（Shell 特有规则！）
- 第4章：函数的实战——改造菜单计算器
- 第5章：函数参数 $1 $2（让函数变"活"）
- 第6章：函数返回值 return（进阶）
- 第7章：local 局部变量（进阶）
- 第8章：综合案例 + 易错点自查表

---

# 第1章 函数是什么？为什么需要它？

## 1.1 函数是什么
**函数 = 给一段代码起个名字，以后想用就喊名字，不用重新写一遍。**

```bash
函数名() {
    要打包的代码
}
```

## 1.2 为什么需要函数？（重点！这是今天搞懂的核心）

**函数解决的不是"能不能干"，而是代码"能不能维护、能不能复用"。**

### 场景1：改一处 vs 改八处
假如"显示菜单"的代码在程序里出现了 8 处：
- ❌ 没有函数：菜单要加一行，得跑到 8 个地方去改，漏一处就出 bug
- ✅ 有函数：菜单只写在函数里 1 处，只改 1 处，全程序自动生效

**一句话：函数让"改一处"而不是"改八处"。**

### 场景2：一次编写，到处调用
一段"检查磁盘空间"的 15 行代码，备份脚本要用、清理脚本要用、报表脚本也要用：
- ❌ 没有函数：三份文件各复制一份 = 45 行重复代码，改逻辑要改三处
- ✅ 有函数：写成一个函数，三份脚本都调用它，改逻辑只改一处

**一句话：函数让代码可以"一次编写，到处调用"。**

### 场景3：代码可读性（函数名就是说明书）
```bash
# ❌ 没有函数：一堆看不懂的代码堆在一起
# ✅ 有函数：一眼看懂脚本干了什么
check_config
backup_files
compress_data
upload_server
send_mail
```

### 打个比方（帮助记忆）
函数就像**厨房里的调料瓶**：
- 不用函数 = 每次做菜都现场从原料开始磨（重复劳动）
- 用函数 = 提前把调料装进瓶子贴好标签，每次做菜拧开就用

---

# 第2章 函数的定义语法

## 2.1 两种写法（推荐第1种）

```bash
# 写法1：带 function 关键字
function 函数名() {
    命令
}

# 写法2：省略 function 关键字（最常用）
函数名() {
    命令
}
```

## 2.2 例子：最简单的函数

```bash
#!/bin/bash
# 定义函数
function say_hello() {
    echo "你好！欢迎来到 Shell 世界"
}

# 调用函数（调用3次 = 执行3次）
say_hello
say_hello
say_hello
```

运行结果：
```
你好！欢迎来到 Shell 世界
你好！欢迎来到 Shell 世界
你好！欢迎来到 Shell 世界
```

---

# 第3章 函数的调用（Shell 特有规则！⚠️）

## 3.1 调用 = 直接写函数名，不加括号！

```bash
# ✅ 正确调用：直接写名字
say_hello

# ❌ 错误调用：加了括号 = 变成"再定义一个函数"，会语法报错！
say_hello()
```

**这是 Shell 和 Python、Java 等语言最大的区别：**
- Python/Java 里调用要写 `函数名()` 
- **Shell 里调用就是函数名本身**，加了括号反而是"定义"的语法

## 3.2 重要规则：定义必须在调用之前

Shell 从上往下执行，必须先"认识"函数才能调用它：

```bash
#!/bin/bash
say_hello          # ❌ 错误！此时还没定义
say_hello() {
    echo "hi"
}
```

```bash
#!/bin/bash
say_hello() {      # ✅ 先定义
    echo "hi"
}
say_hello          # ✅ 后调用
```

---

# 第4章 函数实战：改造菜单计算器

## 4.1 改造前（没有函数，菜单代码在循环里重复出现）

```bash
#!/bin/bash
while [ 1 -eq 1 ]
do
    echo "===== 计算器菜单 ====="     # ← 这几行每轮都写一遍
    echo "1. 加法"
    echo "2. 退出"
    read -p "选择哪个：" n
    case $n in
        1) ... ;;
        2) exit ;;
        *) ... ;;
    esac
done
```

## 4.2 改造后（函数版，菜单只写一处）

```bash
#!/bin/bash
# 把"显示菜单"打包成函数
function show_menu() {
    echo "===== 计算器菜单 ====="
    echo "1. 加法"
    echo "2. 退出"
    read -p "选择哪个：" n
}

while [ 1 -eq 1 ]
do
    show_menu        # 一行调用，代替原来三行 echo！
    case $n in
        1)
            read -p "输入两个数：" a b
            sum=$(( a + b ))
            echo "结果是：$sum"
            ;;
        2)
            echo "再见！"
            exit
            ;;
        *)
            echo "输入无效！"
            ;;
    esac
done
```

**改造后的好处**：以后菜单要加一行"3.乘法"，只改函数里的 1 处，全程序自动生效。

## 4.3 重要知识点：函数里的变量默认是"全局"的

上面的代码里，`n` 是在函数 `show_menu` 内部通过 read 读的，
但函数外面的 `case $n` 依然能拿到它——**因为 Bash 里函数内定义的变量默认全局有效**（函数执行完，变量在外面依然存在）。

```bash
#!/bin/bash
test_global() {
    x=100        # 函数里定义 x
}
test_global
echo $x          # 输出 100 —— 函数外面也能用！证明它是全局的
```

---

# 第5章 函数参数 $1 $2（让函数变"活"）

## 5.1 为什么需要参数
现在的函数是"死的"——永远只打印同一句话。
如果函数能**接收外部传进来的数据**，同一个函数就能干不同的事，变成**通用工具**。

## 5.2 语法

```bash
# 定义时：函数体里用 $1 $2 $3... 代表"第1个、第2个、第3个参数"
函数名() {
    echo "第1个参数是：$1"
    echo "第2个参数是：$2"
}

# 调用时：函数名后面跟参数，用空格隔开
函数名 值1 值2
```

## 5.3 例子1：打招呼函数

```bash
#!/bin/bash
greet() {
    echo "你好，$1！欢迎学习Shell"
}

greet 小明
greet 小红
greet 老王
```

运行结果：
```
你好，小明！欢迎学习Shell
你好，小红！欢迎学习Shell
你好，老王！欢迎学习Shell
```

同一个函数，传不同的名字，输出就不同——这就是参数的价值。

## 5.4 例子2：加法函数（参数参与计算）

```bash
#!/bin/bash
add() {
    sum=$(( $1 + $2 ))    # 拿第1、2个参数做加法
    echo "两数之和是：$sum"
}

add 3 5       # 输出：两数之和是：8
add 100 200   # 输出：两数之和是：300
add 7 8       # 输出：两数之和是：15
```

## 5.5 参数相关规则

| 写法 | 意思 |
|------|------|
| `$1` | 第1个参数 |
| `$2` | 第2个参数 |
| `$#` | 一共传了几个参数 |
| `$@` | 所有参数（一个整体看待的列表） |

⚠️ **注意区分**：
- 函数内部的 `$1` 是"调用函数时传的参数"
- 脚本运行时的 `$1`（如 `sh xxx.sh a` 里的 a）是另一回事
- 两者别搞混！

---

# 第6章 函数返回值 return（进阶）

## 6.1 概念
前面学的函数都是"打印结果"。但有时候，我们希望函数**把结果"交还"给脚本**，让脚本继续用这个结果做判断、做计算。

## 6.2 return 语法

```bash
函数名() {
    return 数字    # 返回一个数字（0-255），表示"函数的结果/状态"
}

函数名            # 先调用
echo $?           # 用 $? 拿到函数返回的值
```

- `return 数字` = 函数结束时"交还"一个数
- `$?` = 上一条命令/函数的返回结果（之前笔记提过）

## 6.3 例子：用 return 判断成绩是否合格

```bash
#!/bin/bash
check() {
    if [ $1 -ge 60 ]
    then
        return 0      # 约定：0 表示"合格"
    else
        return 1      # 约定：1 表示"不合格"
    fi
}

check 85
if [ $? -eq 0 ]
then
    echo "成绩合格！"
else
    echo "成绩不合格！"
fi
```

## 6.4 ⚠️ return 的注意点
- `return` 只能返回**数字**（0-255），不能返回字符串
- 想返回字符串/计算结果，一般用 `echo` + `$( )` 配合：

```bash
# 用 echo 输出结果，外面用 $( ) 接住
get_sum() {
    echo $(( $1 + $2 ))    # 用 echo 把结果"吐"出来
}

result=$(get_sum 3 5)      # $( ) 接住函数输出的内容
echo "结果是：$result"      # 输出：结果是：8
```

这种 `变量=$(函数名 参数)` 的写法，是把函数当"计算器"用，非常常见！

---

# 第7章 local 局部变量（进阶）

## 7.1 概念
之前学了：函数里的变量**默认全局**——函数改了一个变量，外面也会变，可能不小心"污染"外面的同名变量。

`local` 可以让变量**只在函数内部有效**，函数结束就销毁，不影响外面。

## 7.2 语法

```bash
函数名() {
    local 变量名=值    # 声明成局部变量
}
```

## 7.3 例子：对比全局和局部

```bash
#!/bin/bash
# 例1：没有 local（全局）——函数会改掉外面的变量
x=10
change() {
    x=99        # 直接改 x
}
change
echo $x         # 输出 99（外面的 x 被函数污染了！）

# 例2：用了 local（局部）——函数改不到外面
y=10
change2() {
    local y=99  # 只在函数内部有效
}
change2
echo $y         # 输出 10（外面的 y 没被动过！）
```

## 7.4 什么时候用 local
- 函数内部的**临时变量**（如循环计数、中间结果），建议加 `local` 防止污染外面
- 写复杂脚本时是好习惯，让函数"自成一派"

---

# 第8章 综合案例 + 易错点自查表

## 8.1 综合案例：带参数+返回值的函数

```bash
#!/bin/bash
# 做一个"判断数字大小"的函数
compare() {
    if [ $1 -gt $2 ]
    then
        echo "$1 大于 $2"
    elif [ $1 -lt $2 ]
    then
        echo "$1 小于 $2"
    else
        echo "$1 等于 $2"
    fi
}

# 调用测试
compare 5 3      # 5 大于 3
compare 2 8      # 2 小于 8
compare 7 7      # 7 等于 7
```

## 8.2 综合案例：函数 + 循环 + 参数（打印乘法表的一部分）

```bash
#!/bin/bash
# 打印从1到传入数字的平方
print_square() {
    for (( i=1; i<=$1; i++ ))
    do
        echo "$1 的平方是：$(( i * i ))"
    done
}

print_square 5
```

## 8.3 易错点自查表

| # | ❌ 错误 | ✅ 正确 | 原因 |
|---|--------|--------|------|
| 1 | `say_hello()` 调用 | `say_hello` | 调用不加括号，加括号=定义 |
| 2 | 调用写在定义前面 | 先定义后调用 | Shell 从上往下执行 |
| 3 | 函数里 read 忘变量名 | `read -p "..." n` | 没变量名就存进 $REPLY，外面取不到 |
| 4 | 忘写 `}` 结束函数体 | 函数体用 `{ }` 包住 | 结构不完整 |
| 5 | `return 字符串` | `return 数字` | return 只能返回 0-255 的数字 |
| 6 | 想返回字符串用 return | 用 `echo` + `$( )` | 字符串要用 echo 吐出来接住 |
| 7 | 函数内外同名变量互相污染 | 内部用 `local` 声明 | 局部变量不污染全局 |

## 8.4 函数学习一句话总结

> **定义用 `名字() { }`，调用光写名字不加括号，
> 传参用 `$1 $2`，返回数字用 `return` + `$?`，
> 返回内容用 `echo` + `$( )`，内部临时变量加 `local`。**

---

## ✅ 今天的函数课，浓缩成五句话
1. **函数的意义**：代码写一处、改一处、到处调用（这是今天最重要的认知）
2. **定义**：`函数名() { 命令 }` 或 `function 函数名() { 命令 }`
3. **调用**：直接写函数名，**不加括号**（Shell 独有！）
4. **全局变量**：函数里定义的变量默认全局，外面能用
5. **还没练完的**：参数 `$1 $2`（讲了概念）、return、local（进阶，下次练）