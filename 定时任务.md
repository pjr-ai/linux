# Linux 定时任务 & 磁盘挂载 学习笔记

> 📅 学习日期：2026.9.5
> 📚 配套教材：韩顺平 Linux 课程

---

## 📌 目录
1. [crontab 定时任务调度](#一crontab-定时任务调度)
2. [at 一次性任务](#二at-一次性任务)
3. [磁盘分区与挂载](#三磁盘分区与挂载)
4. [今日易错点总结](#-今日易错点总结)

---

## 一、crontab 定时任务调度

### 💡 核心概念
> **让系统在指定时间，自动执行指定命令（周期性重复执行）。**
> 靠后台的 **crond** 服务一直在跑，时间一到就自动执行。

### 1️⃣ crond 服务管理

```bash
systemctl start crond      # 启动 crond 服务
systemctl enable crond     # 设置开机自启
systemctl status crond     # 查看服务状态
```

### 2️⃣ 三个常用命令

| 命令 | 作用 | 记忆 |
|---|---|---|
| `crontab -e` | 编辑定时任务（打开 vim 编写） | e = edit 编辑 |
| `crontab -l` | 查看当前用户的定时任务 | l = look 查看 |
| `crontab -r` | 删除全部定时任务 | r = remove 删除 |

### 3️⃣ 任务格式（⭐⭐ 核心必考）

**五个时间位 + 要执行的命令：**

```
分  时  日  月  周    要执行的命令
*   *   *   *   *    date >> /home/time.log
```

| 位置 | 含义 | 取值范围 |
|---|---|---|
| 第1位 | 分 | 0 ~ 59 |
| 第2位 | 时 | 0 ~ 23 |
| 第3位 | 日 | 1 ~ 31 |
| 第4位 | 月 | 1 ~ 12 |
| 第5位 | 周 | 0 ~ 7（0 和 7 都代表周日）|

### 4️⃣ 每个位置能填什么

| 写法 | 含义 | 举例 |
|---|---|---|
| `*` | 每个都执行 | 时位写 `*` = 每小时 |
| `*/n` | 每 n 个执行一次 | `*/5` = 每 5 分钟 |
| 具体数字 | 指定的那个时间 | 时位写 `2` = 凌晨 2 点 |
| 逗号 `,` | 多个时间点 | 分位写 `1,15` |
| 短横 `-` | 一段范围 | 周位写 `1-5` = 周一至周五 |

### 5️⃣ 经典例子速查

```bash
30 2 * * *  命令        # 每天凌晨 2:30
*/5 * * * * 命令        # 每 5 分钟
0 0 * * *   命令        # 每天 0 点整
0 12 * * 1  命令        # 每周一 12:00
15 6 * * 1-5 命令       # 周一至周五 6:15
```

### 6️⃣ 实战完整流程（⭐⭐ 最易错）

> ⚠️ **crontab 命令和任务内容不能写在同一行！**

```bash
# 第 1 步：终端敲 crontab -e，进入 vim
crontab -e

# 第 2 步：在 vim 里按 i 进入编辑，输入任务那一行，例如：
0 2 * * 1-5 tar -zcvf /home/backup.tar.gz /home/hello.log

# 第 3 步：按 ESC → 输入 :wq 保存退出

# 第 4 步：验证任务是否写入成功
crontab -l
```

> 💡 补充：`crontab -e` 打开的就是 **vim**，正好用上学的 `i` 编辑、`ESC`、`:wq` 保存那套操作。

### 7️⃣ 易错点

- ❌ 任务行里**不要再写** `crontab` 命令本身
- ❌ `crontab -e` 和任务内容**不能在同一行**
- ❌ 压缩后缀是 `.tar.gz`，**不是** `.tar.ga`
- ⚠️ 命令建议写**完整路径**（如 `/bin/date`），避免环境变量问题

---

## 二、at 一次性任务

### 💡 核心概念
> **at = 只执行一次**的任务。
> 对比记忆：crontab 是"闹钟"（周期重复响），at 是"倒计时"（响一次就完事）。

### 1️⃣ at 服务管理（⚠️ 服务名叫 atd，不是 at！）

```bash
systemctl start atd      # 启动 at 服务
systemctl enable atd     # 开机自启
```

### 2️⃣ 设置一次性任务完整流程

```bash
at 15:00            # 回车，进入输入命令状态
at> ls -l /home     # 输入要执行的命令（可写多条）
at> Ctrl + D        # 结束输入，出现 job 编号即创建成功
```

### 3️⃣ 查看 / 删除

```bash
atq             # 查看待执行任务（能看到 job 编号）
atrm 任务编号    # 删除指定任务
```

### 4️⃣ at 的时间写法

```bash
at 15:00                # 今天 15:00
at now + 1 minute       # 1 分钟后
at now + 1 hour         # 1 小时后
at now + 2 days         # 2 天后
at 15:00 2026-09-01     # 指定某天某时
```

### 5️⃣ crontab vs at 对比表（⭐⭐ 常考）

| 功能 | crontab | at |
|---|---|---|
| 编辑 | `crontab -e` | `at 时间` |
| 查看 | `crontab -l` | `atq` |
| 删除 | `crontab -r` | `atrm 编号` |
| 服务 | crond | **atd** |
| 执行特点 | 周期重复 | **只执行一次** |
| 结束保存 | vim 里 `:wq` | 命令行里 `Ctrl + D` |

---

## 三、磁盘分区与挂载

### 💡 核心概念
> **挂载 = 把磁盘分区和一个目录绑定。**
> 绑定之后，访问这个目录 = 访问这块磁盘。

### 1️⃣ 查看磁盘的三个命令

| 命令 | 作用 |
|---|---|
| `lsblk` | 查看所有磁盘、分区结构（树状，最直观）|
| `df -h` | 查看分区使用情况（容量/已用/挂载点）|
| `fdisk -l` | 查看磁盘详细信息（需 root）|

### 2️⃣ 设备名规则

- `sda` = 第一块硬盘，`sdb` = 第二块硬盘
- `sda1`、`sda2` = 该硬盘上的第 1、2 个分区
- 分区需要先建文件系统（**格式化**）才能挂载使用

### 3️⃣ 挂载完整流程（预览）

```
① lsblk 查看磁盘
② fdisk /dev/sdb 分区（交互操作）
③ mkfs.ext4 /dev/sdb1 格式化（建文件系统）
④ mkdir /mnt/xxx 建挂载点目录
⑤ mount /dev/sdb1 /mnt/xxx 挂载
⑥ df -h 验证挂载成功
```

### 4️⃣ 手动挂载命令

```bash
mount /dev/sdb1 /mnt     # 挂载（绑定到 /mnt 目录）
umount /mnt              # 卸载（解除绑定）
```

### 5️⃣ ⚠️ 大坑预警（重点）

> **手动 `mount` 挂载，重启后会失效！**
> 要实现开机自动挂载，需要配置 `/etc/fstab` 文件（后面会重点学）。

---

## 📌 今日易错点总结

| # | 易错点 | 正确做法 |
|---|---|---|
| 1 | crontab 任务里写了 `crontab` 命令 | 任务行只写：时间 + 命令 |
| 2 | 后缀写成 `.tar.ga` | 正确的是 `.tar.gz` |
| 3 | at 服务写成 `at` | 服务名是 **atd** |
| 4 | at 结束按单个 `d` | 要按 **`Ctrl + D`** |
| 5 | 混淆 mkdir 参数 | mkdir 递归用 **`-p`**（不是 `-r`）|
| 6 | 磁盘格式名 | ext4 / xfs |