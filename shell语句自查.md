# 📕 Shell 错题本 —— 我的专属踩坑记录
### 对应六道闯关练习，按"错题 → 正确 → 知识点"逐条复盘

> 使用方法：每道题先看自己**当时写错的代码**，回忆一下错在哪，
> 再看正确写法，最后精读"知识点"——那才是以后不再犯错的根本。

---

## 📌 通病总览（先看这个！我重复犯的错）

练了六道题，我反复在**同一个坑**里跌倒。先列出五大通病，后面每题还会细讲：

| # | 我的通病 | 出现的题号 |
|---|---------|-----------|
| 1 | `read` 要么忘加 `-p`，要么忘写接收变量 | 第1题、第6题 |
| 2 | **赋值时**等号左边加了 `$`（`$sum=` 是错的） | 第3题、第4题 |
| 3 | 算术 `$(( ))` 用法混乱：`=` 塞进计算器、`$` 摆错位置、用老式 `$[ ]` | 第3题 |
| 4 | for 和 while 的结构搞混 | 第4题、第5题 |
| 5 | `echo"文字"` 忘加空格，被当成一个不存在的命令 | 第3题、第4题 |

> 一句话总结我的核心毛病：
> **"取变量的值"（要加 $）和"给变量存值"（不能加 $）这两个动作，我总是搞反。**
> 这篇错题本第3题部分会把这个讲透。

---

# 第1题复盘：成绩判断（if + read）

## ❌ 我的错误代码（初版）

```bash
#! /bin/bash
read  "请输入你的成绩 $score"        # ① 见错误1
if [ $score -gt 90 ]                # ② 见错误2 ④
echo "优秀"                          # ③ 见错误3
elif [ $score -gt 60 ]
echo "及格"
else [ $score -lt 60 ]               # ③ 见错误3
echo  "不及格"
fi
```

我犯的错可不少：read 用错、if 缺 then、else 带条件、分数线用错、还出现过 `$score` 和 `$SCOre` 大小写不一致。

## ✅ 正确写法

```bash
#!/bin/bash
read -p "请输入你的成绩：" score
if [ $score -ge 90 ]
then
    echo "优秀"
elif [ $score -ge 60 ]
then
    echo "及格"
else
    echo "不及格"
fi
```

## 📖 知识点详解

### 知识点A：`read` 的完整格式（我反复错，重点！）

```bash
read 变量名               # 最简：只接收输入
read -p "提示文字" 变量名  # 常用：先显示提示，再等输入
```

`read` 命令由**两部分**组成，缺一不可：
1. `-p "提示文字"` → 显示在屏幕上给用户看的（可有可无，但要有就用 `-p`）
2. **变量名** → 用户输入的内容**存到哪里**（必须有！）

我当时两个错：
- 忘了写 `-p` → 那串中文被当成变量名，不显示提示
- 忘了写接收变量 → 输入内容存进系统默认的 `$REPLY`，我后面判断用的 `$score` 永远是空的 → **永远输出"不及格"**

### 知识点B：变量是"区分大小写"的

```bash
score=85
echo $score    # 85
echo $SCOre    # 空！（SCOre 和 score 是两个不同的变量）
```

`$score` 和 `$SCOre` 不是同一个东西。Shell 里**大写小写是严格区分的**，写错一个字母，那个变量就是"未定义"，取出来是空的。

### 知识点C：`if` 的结构——条件后面必须有 `then`

```bash
if [ 条件 ]          # 判断
then                 # "那么"——必须有！
    命令
fi                   # 结束标记——必须有！
```

`then` 是 if 的"开闸门"，没有它，后面的命令不属于 if 管，结构直接坏掉。
想写一行可以：`if [ 条件 ]; then`（用 `;` 代替换行）。

### 知识点D：`else` 后面不能再跟条件

```bash
else            # "剩下的所有情况"——它自己就代表"都不满足"
    命令
```

`else` 的意思是"前面 if/elif 全不成立时，剩下的**所有**情况都归我"。
所以 else 后面**直接写命令**，不能再写 `[ 条件 ]`。要再细分情况，应该用 `elif` 而不是 else。

### 知识点E：`-gt`（大于）和 `-ge`（大于等于）的边界

| 运算符 | 意思 | 90分会进哪个分支 |
|--------|------|----------------|
| `-gt 90` | 严格大于90 | 90 分**进不了**优秀（漏掉） |
| `-ge 90` | 大于等于90 | 90 分**能进**优秀 ✅ |

题目说"≥90 优秀"，就必须用 `-ge`。用 `-gt` 的话，考90分的人会被错误地判成"及格"。

**记忆口诀**：`e` = equal（等于）。`-ge` 里有 e = 包含等于；`-gt` 没有 e = 不包含。

---

# 第2题复盘：星期选择（case）

## ❌ 我的错误代码

```bash
case SUM in          # ① 变量忘加 $ 见错误1
...
'$*')                # ② 见错误2、错误3
echo "输入无效"
esac
```

## ✅ 正确写法

```bash
#!/bin/bash
read -p "输入一个数字1-3" SUM
case $SUM in
1)
    echo "星期一"
    ;;
2)
    echo "星期二"
    ;;
3)
    echo "星期三"
    ;;
*)
    echo "输入无效"
    ;;
esac
```

## 📖 知识点详解

### 知识点A：case 后面要跟"变量的值"，所以要加 `$`

```bash
case $SUM in    # ✅ 取 SUM 的值去匹配
case SUM in     # ❌ 拿字母 S-U-M 去匹配，永远匹配不上！
```

和 echo 一样，**取变量的值必须加 `$`**。不加 `$`，case 就是拿字面文字 "SUM" 和分支比较，用户输入 1/2/3 永远进不去。

### 知识点B：case 的完整结构（背下来）

```bash
case $变量 in
"值1")
    命令1
    ;;          # 双分号：这一支结束
"值2")
    命令2
    ;;
*)              # 星号：匹配"其他所有情况"= else
    默认命令
    ;;
esac            # 结束标记！是 case 倒过来写，不是 fi！
```

三个容易漏的点：
1. 每支结尾要写 `;;`（两个分号，不是 `;` 一个）
2. 最后要有 `*)` 兜底（相当于 else）
3. 结尾是 **esac**（case 反着拼）

### 知识点C：`*` 是"通配符"，不是变量，不能加 `$`

```bash
*)      # ✅ 一个光秃秃的星号 = 匹配一切
$*)     # ❌ $ 表示"取变量的值"——取一个叫 * 的变量？取出来是空的，兜不住任何情况
```

`$` 的职责是"取变量值"。而 `*` 在这里不是变量，是**通配符**（代表"任何东西"）。所以写 `$*` 就闹了笑话——Shell 去找一个叫 `*` 的变量，找不到，返回空。

### 知识点D：单引号 `'...'` 会让内容"原样照打"

```bash
'$*'    # 单引号里的 $ 失去特殊含义，变成普通字符
"$*"    # 双引号里 $ 仍会被翻译
*       # 不加引号，* 保持通配符身份
```

我当时写 `'$*')`，等于告诉 Shell："去匹配一个**字面上叫 $* 的文字**"——当然什么都匹配不上。
**教训：通配符 `*` 就光秃秃地写，别加引号，别加 `$`。**

---

# 第3题复盘：for 循环求和（最难的一题）

## ❌ 我的错误过程（一路改了好几版）

```bash
$ (( $sum = i+sum ))      # 版1：四处全乱
$ (( sum = i+sum ))       # 版2：$ 摆错位置、= 进了计算器
$(( i+sum ))              # 版3：计算器对了，但没人"接住"结果
$sum=$(( i+sum ))         # 版4：等号左边又带上了 $
sum=$[ i+sum ]            # 版5：写法对了，但用了过时的 $[ ]
for [ i=1;i<n;i+1 ]       # 版6：for 语法整个错
```

## ✅ 正确写法

```bash
#!/bin/bash
sum=0
read -p "输入一个整数：" n
for (( i=1; i<=n; i++ ))
do
    sum=$(( sum + i ))
done
echo "1到n的总和是：$sum"
```

## 📖 知识点详解（第3题是知识密度最高的一题，务必精读）

### 知识点A：`$` 的两副面孔——"取"和"存"绝不能搞反 ⭐ 最重要

Shell 里 `$` 只有**一个**用途：**取变量的值**。

```bash
echo $sum          # 取：把 sum 的值拿出来用 → 加 $
sum=$((...))       # 存：把结果放进 sum 盒子 → 不加 $！
$sum=$((...))      # ❌ 大错！"取"和"存"同时发生，语法不合法
```

**记忆方法（盒子比喻）**：
- 把变量想成**盒子**，`$` 是"**从盒子里掏东西**"的手势
- **往盒子里放东西**（赋值）：只要写盒子名 `sum`，别伸手去掏
- **从盒子里拿东西用**（echo、判断、比较）：才伸手 `$sum`

我当时反复在"放东西"的时候写了 `$sum=`，这就是拿"取"的手势去干"存"的活。

### 知识点B：算术计算器 `$(( ))` 的正确用法

**完整格式**（三个部分，顺序不能乱）：

```bash
sum=$(( sum + i ))
│    │       │
│    │       └─ ③ 计算器内部：只写算式，变量不用加 $，也不能写 =
│    └────────── ② 计算器：$ 和 (( 必须紧贴，不能有空格
└─────────────── ① 盒子名（不带$）+ 等号：负责"接住结果"
```

我犯的错逐个对照：
- `$ (( ... ))` → `$` 和 `((` 之间有空格 → 语法错。**`$((` 是一个整体，必须紧贴**
- `$(( sum = i+sum ))` → 把 `=` 写进了计算器。**计算器只负责"算"，"存"是外面 `=` 的活**，一山不容二虎
- `$(( $sum + i ))` → 计算器**内部**已经认识变量，不用再加 `$`
- `sum=$(( i+sum ))` → ✅ 标准正确写法

### 知识点C：`$(( ))` 是标准，`$[ ]` 是过时写法

```bash
sum=$(( sum + i ))   # ✅ 标准，推荐，所有 bash 都支持
sum=$[ sum + i ]     # ⚠️ 老式写法，能用但已过时，应避免
```

我当时图省事用了 `$[ ]`，虽然能跑，但**好习惯要用标准写法** `$(( ))`。

### 知识点D：for 循环的正确语法（两种）

**语法一：遍历列表**
```bash
for 变量 in 值1 值2 值3
do
    命令
done
```

**语法二：C 风格（从几到几，最常用）** ← 我该用这种
```bash
for (( 初始值; 循环条件; 每轮变化 ))
do
    命令
done
```

我错写成了 `for [ i=1;i<n;i+1 ]`，三个错误：
1. **用了 `[ ]`** → `[ ]` 是条件判断用的！for 的"从几到几"必须用**双小括号 `(( ))`**
2. **`i+1` 不是自增** → `i+1` 只是"算了个加法"，结果**没存回 i**，i 永远不变 → 死循环！正确是 `i++`（i 自己加1）或 `i=$(( i+1 ))`
3. **`i<n` 边界错** → 求 1 加到 n，n 本身要算进去，应该 `i<=n`（小于**等于**）

### 知识点E：累加的标准套路（背下来）

```bash
sum=0                       # 1. 先准备"存钱罐"，初始为 0
for (( i=1; i<=n; i++ ))    # 2. i 从 1 跑到 n
do
    sum=$(( sum + i ))      # 3. 每圈：把 sum 当前值 + i，结果存回 sum
done
```

理解 `sum=$(( sum + i ))`：
- 右边计算器先算"现在 sum 里的值 加上 i"
- 左边 `sum=` 把结果**存回 sum 盒子**（覆盖旧值）
- 就像往存钱罐里一枚枚投硬币：第1圈 sum=1，第2圈 sum=1+2=3，第3圈 sum=3+3=6……

---

# 第4题复盘：while 循环打印 1 到 n

## ❌ 我的错误过程

```bash
$n=0                                   # ① 赋值又带 $（第3题的病又犯了）
while [ i=1;i<=n;i++ ]                 # ② 把 for 的写法塞进 while
while [ i<=n ]                         # ③ 条件里忘加 $、用了 <=
i=i++                                  # ④ 不会自增
```

## ✅ 正确写法

```bash
#!/bin/bash
i=1
read -p "正整数" n
while [ $i -le $n ]
do
    echo "$i"
    i=$(( i + 1 ))
done
```

## 📖 知识点详解

### 知识点A：for 和 while 最大的区别——"三件套"放的位置 ⭐

循环都要做三件事：**①从几开始 ②到几停 ③每轮怎么变**

**for：三件套全塞进一个括号，一行搞定**
```bash
for (( i=1; i<=n; i++ ))    # ① ② ③ 都在这里
```

**while：三件套要分三个地方放**
```bash
i=1                         # ① 初始化 → 放在 while 前面！
while [ 条件 ]              # ② 条件 → 只放这里
do
    循环体
    i=$(( i+1 ))            # ③ 更新 → 放在 do...done 里面！
done
```

我把 `while [ i=1;i<=n;i++ ]` 这种 for 写法套进 while，就是没分清：
**while 后面只允许放"判断条件"，不允许放初始化和更新。**

### 知识点B：`[ ]` 条件判断里的规则（很重要）

`[ ]` 里面**不是随便写**的，必须遵守：
1. **变量要加 `$`**：`[ $i -le $n ]`，不能写 `[ i -le n ]`（否则拿字母 i 去比）
2. **不能用数学符号** `<=` `<` `>` `=`，要用专门的运算符：

| 数学写法 | [ ] 里正确写法 | 意思 |
|---------|---------------|------|
| `<=` | `-le` | 小于等于 |
| `<` | `-lt` | 小于 |
| `>=` | `-ge` | 大于等于 |
| `>` | `-gt` | 大于 |
| `==` | `-eq` | 等于 |
| `!=` | `-ne` | 不等于 |

我当时写 `[ i<=n ]`，两个错全占：忘加 `$`、用了 `<=`。

### 知识点C：`i=i++` 为什么是错的

```bash
i=i++        # ❌ Shell 把它理解为：把一个叫 "i++" 的字符串存进 i！
i=$(( i+1 )) # ✅ 用计算器真正做加法，结果存回 i
i=$((i+1))   # ✅ 不加空格也能跑（规范写法加空格更好看）
```

`i++` 这种写法只有在 `$(( ))` 或 `for (( ))` 里才有"自增"的含义。单独写 `i=i++`，`=` 右边就是一个普通字符串 "i++"，i 永远不变 → **死循环**。

### 知识点D：防死循环意识 ⚠️

while 循环里**只要条件永远为真**就会死循环。防止方法：
1. 循环体里必须有让条件**变量变化**的语句（如 `i=$(( i+1 ))`）
2. 万一屏幕疯狂刷屏 → 立刻按 **`Ctrl + C`** 强制终止

---

# 第5题复盘：菜单计算器（while + case 综合）

## ❌ 我的错误过程（两版都有问题）

```bash
#!bin/bash                  # ① 少了 /
$sum                        # ② 单独一行的废代码
i=1
while [ $i -le 3 ]          # ③ 转3圈自动停，不符合要求
...
case [ n ] in               # ④ case 用了 [ ]
1)
    read -p "输入两个数" a b
    do                      # ⑤ case 分支里混进 do...done
        sum=$(( a+b))
        ...
    done
;;
2)
    echo "再见！"           # ⑥ 没 exit，选2退不出去
;;
...
i=$(( i+1 ))
done                        # ⑦ 早期版本还漏了 esac
```

## ✅ 正确写法

```bash
#!/bin/bash
while [ 1 -eq 1 ]           # 永远成立 → 循环永不自己停
do
    echo "===== 计算器菜单 ====="
    echo "1. 加法"
    echo "2. 退出"
    read -p "选择哪个：" n
    case $n in
        1)
            read -p "输入两个数：" a b
            sum=$(( a + b ))
            echo "结果是：$sum"
            ;;
        2)
            echo "再见！"
            exit            # 选 2 → 真正结束整个脚本
            ;;
        *)
            echo "输入无效！"
            ;;
    esac
done
```

## 📖 知识点详解

### 知识点A：脚本第一行——声明头（shebang）

```bash
#!/bin/bash    # ✅ 正确：#! 和 /bin/bash
#!bin/bash     # ❌ 少了斜杠 /
```

第一行 `#!/bin/bash` 是告诉系统"用 bash 来解释这个脚本"。`#!` 之后要跟**绝对路径** `/bin/bash`，漏了 `/` 就找不到解释器。

### 知识点B：`case` 后面跟变量值，不用 `[ ]`

```bash
case $n in     # ✅ [ ] 是判断用的，case 这里不要！
case [ n ] in  # ❌ 画蛇添足，语法错
```

`[ ]` 的职责是"条件判断"（配合 if/while 用）。case 的职责是"拿变量的值去匹配分支"，所以 case 后面就是**光秃秃的 `$变量`**。

### 知识点C：`do...done` 是循环专属，不能乱用

```bash
do ... done    # 只属于 for 和 while！
```

我在 case 的**分支1**里塞了一对 `do...done`，那里根本没有循环——那两行是多余的，必须删掉。**判断 do/done 该不该出现：看看有没有 for 或 while 跟它配对。**

### 知识点D：怎么让菜单"一直循环，直到选 2 才退出"

这是这题的核心设计，两个新零件：

**零件1：死循环的正确写法（让 while 永不自己停）**
```bash
while [ 1 -eq 1 ]    # 1 永远等于 1 → 条件永远为真 → 永不自己停
# 也可以：while true（意思一样）
```

**零件2：`exit` —— 立刻结束整个脚本**
```bash
exit    # 不管在哪一层（while 里、case 里），碰到就整个脚本结束
```

组合逻辑：
- 循环**永远不会自己结束**（条件永远真）
- 什么时候结束？**由 exit 决定**——用户选 2，分支2 里 `exit`，脚本立刻终止
- 用户选 1 或其它 → 没有 exit → 转完一圈回到 while → 重新显示菜单

我当时用 `while [ $i -le 3 ]` + `i=$(( i+1 ))` 数圈数——这是"固定转3圈"，用户想算第4次都不行，而且选2也不会停（只打印"再见"就回到菜单）。**方向错了：该用"永远循环 + exit 主动退出"，不是"数圈数自动停"。**

---

# 第6题复盘：猜数字游戏（终极综合）

## ❌ 我的错误代码

```bash
read "随机写一个数字" n        # ① read 又忘加 -p！（第1题的病复发）
if (( $n=$secret ))           # ② 一个 = 是赋值，不是比较！
```

## ✅ 正确写法

```bash
#!/bin/bash
secret=$(( $RANDOM % 10 + 1 ))   # 生成 1-10 随机数
while [ 1 -eq 1 ]                # 猜不对就一直猜
do
    read -p "猜一个 1-10 的数字：" n
    if (( $n == $secret ))       # == 才是"相等比较"
    then
        echo "恭喜你，猜对了！"
        exit
    elif (( $n < $secret ))
    then
        echo "太小了，再试一次"
    elif (( $n > $secret ))
    then
        echo "太大了，再试一次"
    fi
done
```

## 📖 知识点详解

### 知识点A：`=` 是赋值，`==` 才是比较 ⭐ 致命bug

```bash
if (( $n = $secret ))     # ❌ 一个 = ：把 secret 的值塞进 n！
                          #    塞完 n 就等于 secret 了，条件永远成立 → 必赢bug
if (( $n == $secret ))    # ✅ 两个 = ：才是"相等吗？"的比较
```

这是猜数字游戏**最致命**的坑：
- `=` 是**赋值**（把右边塞进左边），这个动作**永远成功**，条件永远为真
- 结果：不管用户猜什么，第一轮就打印"猜对了"退出，游戏毫无意义
- **算术比较里：相等用 `==`（两个等号）**；`<`、`>` 在 `(( ))` 里可以直接用

补充：如果不用 `(( ))` 而用 `[ ]` 风格，相等比较用 `-eq`：
```bash
if [ $n -eq $secret ]    # [ ] 风格：用 -eq
if (( $n == $secret ))   # (( )) 风格：用 ==
```
两种风格都行，但**别混着用**。

### 知识点B：`read -p` 又忘了——老毛病复发

```bash
read -p "提示文字" 变量名    # ✅
read "提示文字" 变量名       # ❌ 没 -p，中文被当成变量名
```

这是我**第二次**犯这个错了（第1题一次、第6题一次）。说明这种细节容易反复忘。
**对策：以后写 read，先默念口诀"read 减 p 引号 变量名"——`read -p "..." var`。**

### 知识点C：随机数生成（这题我一次写对了，表扬自己）

```bash
$RANDOM              # bash 内置"随机数源"，每次取值得到 0~32767
$RANDOM % 10         # % 是取余数 → 得到 0~9
$RANDOM % 10 + 1     # +1 → 变成 1~10 ✅
secret=$(( $RANDOM % 10 + 1 ))
```

`%`（取余数）的用途：把一个**大范围随机数**缩到**小范围**。`X % 10` 的结果一定是 0~9，再加 1 就是 1~10。

### 知识点D：综合题的"退出设计"回顾

这题我用了**第5题学会的死循环 + exit** 套路：
- `while [ 1 -eq 1 ]` 让循环永不自己停
- 猜对时 `exit` 立刻结束
- 猜错时没有 exit → 回到循环 → 继续 read → 继续判断

⚠️ 注意：这题的 if 我放在了 while **里面**（每猜一次判断一次），和菜单题的结构不同——菜单题是 case 在 while 里，这题是 if 在 while 里。**灵活套用**才是综合题的真谛。

---

# 🎯 终极总结：六道题浓缩成一张"避坑卡"

## 我的五大高频错误 & 根治口诀

| # | 高频错误 | 根治口诀 |
|---|---------|---------|
| 1 | `read` 忘 `-p` 或忘变量名 | **"read 减p 引号 变量名"**：`read -p "..." var` |
| 2 | 赋值时左边加 `$` | **存值不带 $，取值才带 $**（盒子比喻：放东西不掏手） |
| 3 | `$(( ))` 里写 `=`、乱放 `$` | **计算器只管算，存值靠外面**：`sum=$(( sum+i ))` |
| 4 | for/while 结构混淆 | **for：三件套一个括号**；**while：初始化在外、条件在[ ]、更新在体内** |
| 5 | `echo"..."` 没空格 | **命令和内容之间永远有空格**：`echo "..."` |

## 关键字开关自查表（写错必报语法错）

| 结构 | 开始 | 结束 | 备注 |
|------|------|------|------|
| if | `if [ 条件 ]` | `fi` | 条件后要有 `then` |
| case | `case $var in` | `esac` | 每支结尾 `;;`，兜底 `*)` |
| for | `for ((...))` | `done` | 条件在括号里 |
| while | `while [ 条件 ]` | `done` | 条件只放判断 |

**结束标记的记忆技巧**：`fi` 是 `if` 倒写，`esac` 是 `case` 倒写，`done` 是 do 的完成时——**看到开头就要想到它的倒写结尾**。

## 符号对照卡（一图记住）

| 动作 | 写法 | 示例 |
|------|------|------|
| 存值（赋值） | 变量名 = ...（不带$） | `sum=0` |
| 取值（使用） | $变量名 | `echo $sum` |
| 算数（计算器） | $(( 算式 )) | `$(( sum + i ))` |
| 数字比较（[ ]风格） | -eq -ne -gt -lt -ge -le | `[ $i -le $n ]` |
| 相等比较（(( ))风格） | == | `(( $n == $secret ))` |
| 赋值（注意！） | = （单个） | `n=5` 是赋值 |
| 其他情况（case兜底） | * | `*)` |
| 字符串相等 | = （单个，在[ ]里） | `[ "$a" = "$b" ]` |
| 立刻结束脚本 | exit | `exit` |

---

> 📌 最后一句心里话：
> 这本错题本里的每一个错，都是我**亲手犯过、又亲手改对**的。
> 犯错不可怕，可怕的是犯完就忘。建议每过几天回来重做一遍这六道题，
> 如果能**一次全对**，说明这些知识点真正长在我身上了。加油！🐚